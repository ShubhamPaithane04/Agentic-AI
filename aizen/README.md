# Aizen Frontend

This folder contains the active React frontend for Aizen.

Run it with:

```bash
cd aizen/frontend
npm install
npm run dev
```

The backend source of truth is the Flask app in `/backend`.
